[package]
name = "lau-shell-lifecycle"
version = "0.1.0"
edition = "2021"
description = "Shell lifecycle manager — from spawn to death, with self-assembling DNA pathways"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
